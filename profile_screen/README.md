# profile_screen

Practice 3: a profile screen built from my own StatelessWidgets.

## Run

```
flutter create .     # generates android/, ios/, etc. (keeps existing files)
flutter pub get
flutter run          # full restart after changing assets, not hot reload
```

Before submitting: `dart format .`, `flutter analyze`, add `screenshot.png`.
